You are an expert analyst. Your job is to evaluate evidence step by step, consider alternatives, and reach a justified conclusion. Reasoning: high.

You are given the following:
- A context (the task description and any initial state given to the agent)
- The agent's trajectory that was labeled as a failure (a sequence of action / observation steps, or a single answer for non-agentic tasks)
{% if correct_answer %}
- A reference
{% endif %}
{% if correct_outputs %}
- One or more successful trajectories from peer agents on the same task
{% endif %}

Your task:

1. Structured Correct Solution: Analyze the successful trajectories (or the reference) and extract the main required criteria or steps that the agent had to satisfy to succeed on the task.

2. Step-by-step Evaluation: Evaluate the failed trajectory against each of the required criteria. For each criterion, provide the following fields:
present_in_wrong: Whether it is present in the failed trajectory
quality: The quality of its execution (correct, partially correct, incorrect, or null if missing)
evidence: Supporting evidence from the failed trajectory (quote a step or action)
comment: Any relevant comments

3. Error Diagnosis: Identify the first major error in the failed trajectory that led to the task being marked as a failure, and provide the following fields in final_answer:
error_summary: If such an error exists, summarize what went wrong. This may be a reasoning weakness (e.g., 'the agent misinterpreted an API response'), a planning error (e.g., 'the agent failed to address part of the task'), a tool-use error (e.g., 'the agent retried the same action without addressing the validation error'), or a control-flow error (e.g., 'the agent terminated before completing all required updates'). Pick the level that best identifies the root cause of the failure.
title: Provide a short, free-form title that describes the specific type of error.
* If the failed trajectory is essentially identical to the successful trajectories on the relevant actions, the failure may be a grading or labeling artifact rather than an agent error — in that case, set error_title to "Label Artifact" and explain in error_summary.
* If you didn't find any error in the failed trajectory, leave all the fields of final_answer with an empty string.
* Avoid ambiguous titles or ones that cannot be mapped to a specific skill or behavior. For example, instead of using "Wrong tool call", identify the underlying error such as "Outcome Misinterpretation", "Premature Termination", or "Constraint Ignorance".

Use as many steps and thinking process as you need. Finally, output the final result in the following format:
{
  "required_criteria": [
    {
      "criterion": "Verify the user's identity before performing account actions",
      "present_in_wrong": true,
      "quality": "correct",
      "evidence": "[step 2] ACT: find_user_id_by_name_zip(...)",
      "comment": "Identity verified via name+zip lookup"
    },
    {
      "criterion": "Address all three issues affecting mobile-data speed (roaming, data saver, VPN)",
      "present_in_wrong": true,
      "quality": "incorrect",
      "evidence": "[step 8] ACT: enable_roaming(...) then [step 12] task ends",
      "comment": "Only enabled roaming; never asked the user about data saver or VPN"
    }
  ],
  "final_answer": {
    "error_summary": "The agent only addressed one of the three required diagnostic steps and terminated without engaging the user in the speed-troubleshooting protocol that the successful trajectories use.",
    "error_title": "Incomplete Diagnostic Protocol"
  }
}


Use the following inputs:

Context:
{{ input_text }}

{% if correct_answer %}
References:
{{ correct_answer }}
{% endif %}

{% if correct_outputs %}
Successful Trajectories:
{{ correct_outputs }}
{% endif %}

Failed Trajectory:
{{ output_text }}

Keeping the evaluation criteria in mind, do not provide a general assessment. Be specific, structured, and evidence-based — quote steps from the failed trajectory.

Assessment:
